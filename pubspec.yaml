name: ma_logic
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: '>=3.0.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
  # アセットはCustomPainter/BoxDecorationでコード描画に移行
  # PNGフォールバック用に残すが、コード描画が優先
  # assets:
  #   - assets/images/hiyoko/
  #   - assets/images/lion/
  #   - assets/images/bond/
